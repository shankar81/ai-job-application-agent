## Imported Claude Cowork project instructions

This is LinkedIn auto apply job project, there is scrapper which uses playwrite to find the jobs and its details, and there is agent build with langgraph, which matches per job with my resume and if matchis more than 6 then we will start auto easy apply flow, also we can also just scrape an d store the job details in xls but this code is commented for now, i am working on auto easy apply feature
